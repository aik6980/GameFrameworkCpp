#include "stdafx.h"
#include "UnitTest.h"

#include "Core/CoreCpp.h"
#include "Core/Math/MathCpp.h"

namespace MathTest
{
	void RunTest()
	{

		/*
		{
			Vec4f v1 = Vec4f( 3.0f, 4.0f, 5.0f, 1.0f );
			Vec4f v2 = Vec4f( 1.0f, 2.0f, 3.0f, 1.0f );

			float f = Dot4(v1, v2);

			Debug::Print(boost::wformat(TEXT("%1%")) % f );
		}

		{
			Vec4f v = Vec4f( 0.2f, 0.2f, 5.0f, 7.0f);
			float f = v.Vec2LengthSq();

			Debug::Print(boost::wformat(TEXT("%1%")) % f );
		}

		{
			Mat4f* mat = new Mat4f();
			Mat4f newMat = Inverse(*mat);
			Debug::Print(boost::wformat(TEXT("%1%")) % mat->Str() );
			delete mat;
		}
		*/
	}
}